# Tasks

Work is organized in three levels: **Phase → Task → SubTask**. Phases are sequential. Preserve task order when checkpointing; completion requires evidence.

| Symbol | Status | Meaning |
| --- | --- | --- |
| ⬜ | pending | 尚未开始 |
| 🔄 | in_progress | 已开始，尚未完成 |
| ✅ | done | 有明确完成证据 |
| ⏸️ | blocked | 已确认的阻塞，需注明原因 |

Task/Phase 状态由 SubTask 推导：有进行中项则为 in_progress；无进行中项而存在阻塞则为 blocked；全部完成才为 done；尚未开始为 pending。当前已开展本机环境与最小推理验证；状态以各任务标记为准。用户明确暂缓的第二台机器核对单独保留，不计入当前 2.1 的关闭条件，也不计作已验证。

## Phase 1 — 方案与上下文初始化（✅ done）

### 1.1 需求与设计整理（✅ done）

- ✅ 确认 CLI 为 MVP 正式入口，手动批量导入图片，自动截图与翻页留到后续。
- ✅ 确认英文文档优先，兼顾表格和少量代码，几乎无手写内容。
- ✅ 确认速度优先，MVP 包含基本排版与轻量词级纠错，深入句子语法和语义留到二次校验。
- ✅ 确认 Markdown 输出及后续 PDF 扩展，记录复杂表格表示方案。
- ✅ 确认 Python 3.12 和后续 uv 初始化；记录两台机器的已知配置。
- ✅ 整理双模型优先验证、单模型对比及按页处理架构。

### 1.2 样图评估与文档保存（✅ done）

- ✅ 目视查看 text.png、fuzzy-photo.png、complex-table.png 和 code.png。
- ✅ 记录摩尔纹、技术名称、表格前导零及代码符号风险；注明现有表格样本不覆盖复杂表格。
- ✅ 初始化五份文档并保存 checkpoint；同步 CLI、无人值守批处理和两轮校验边界。

## Phase 2 — 本地模型可行性验证（🔄 in_progress）

### 2.1 初始化验证环境（✅ done）

- ✅ **2.1.1 恢复仓库与设备核对（当前范围）：** 已核对仓库、样图和当前执行机信息；第二台机器按用户安排暂缓，命令文件已准备好，由用户自行测试核对，不作为当前 2.1 的关闭前提。
- ✅ **2.1.2 检查工具、存储和获取条件：** 核对 uv、Python 3.12、缓存与磁盘空间、目录权限、依赖及模型来源可达性，记录可复用项和缺少项。
- ✅ **2.1.3 核对候选路线与后端兼容组合：** 依据执行时官方资料确定候选版本、后端及 CPU/NVIDIA 验证路径，记录 Python 3.12 支持依据和必要的环境隔离；最终模型选型留到 2.2。
- ✅ **2.1.4 使用 uv 初始化 Python 3.12 项目：** 建立最小项目配置和虚拟环境，生成 pyproject.toml、.python-version，验证实际解释器版本与路径。
- ✅ **2.1.5 安装并锁定验证依赖：** 安装必要依赖并生成 uv.lock，验证关键导入、设备枚举和锁定配置；排除本地缓存、权重与运行输出。
- ✅ **2.1.6 准备候选模型及加载配置：** 准备 PaddleOCR-VL、小型文字模型和 Qwen3-VL 首轮候选，记录来源、版本、路径、设备及精度配置，逐项验证加载。
- ✅ **2.1.7 执行最小本地推理验证：** 在当前机器的所选 CPU 或 NVIDIA 路径上，分别验证视觉模型单图输入和文字模型短文本输入，保存真实命令、配置、非空输出及失败记录；不提前开展完整基线或质量排名。
- ✅ **2.1.8 整理复现说明并保存交接：** 已补齐完整可执行命令文件、各模型组件文件 SHA-256 manifest、新进程推理复现证据和第二台机器的用户测试交接说明；Paddle 路线另有同一推理子进程写入的设备证据；完成后停在 2.2 前。

各子任务的执行步骤、产物和完成条件见 [current.md](current.md) 的“Task 2.1 — 新 session 执行交接”。本机 CPU 最小推理、参数化命令文件和复现交接均已完成；第二台机器核对已按用户安排暂缓，不阻塞当前 2.1 收尾，不能把单机结果记作双机验证完成。

### 2.1 实施证据（2026-09-03）

- 2.1.1 当前范围完成：执行机已实测为 Windows 10 x64、32 GB 级内存、AMD Ryzen AI 9 H 465、Radeon 880M；第二台用户报告的 RTX 3080 机器核对按用户安排暂缓，不作为本次关闭前提。
- 2.1.2–2.1.5 已完成：uv/Python 3.12.13、虚拟环境、依赖、uv.lock、关键导入、设备枚举和新进程同步均已验证；Torch 为 CPU build，未识别 CUDA，Paddle 使用 cpu。
- 2.1.6–2.1.7 已完成当前机验证：PaddleOCR-VL-1.6、Qwen3-4B-Instruct-2507 和 Qwen3-VL-4B-Instruct 均从本地缓存加载并产生非空结果；结果位于 validation-output/，真实命令和配置记录在 docs/environment.md。
- 2.1.8 已完成：已新增 scripts/check_environment.py、scripts/validate_model.py 和 scripts/paddleocr_child.py，记录完整参数化命令、VL 与 PP-DocLayoutV3 模型组件的 SHA-256 manifest、三条脚本化推理和负向失败证据；新进程同步与文档交接已复核。2.2 的人工参考答案、完整基线、质量排名和最终选型未开始。

### 2.1 追溯性纠正（2026-09-03）

- Paddle 路线未传 max_new_tokens 时，run.json 记录为 null，避免把脚本默认值误写成实际命令参数。
- Paddle 推理移入 scripts/paddleocr_child.py；子进程在实际预测完成后写入 actual_device_after_predict，修正后的运行记录确认设备为 cpu。
- Paddle 路线要求显式提供 PP-DocLayoutV3 目录，该目录的完整文件哈希进入 model_components.layout_detection；修正运行产生非空 Markdown，退出码为 0。

### 第二台机器测试核对（暂缓；用户安排）

- 范围：用户报告的 3080 机器系统、硬件/显存、驱动、框架设备识别和实际推理测试。
- 恢复条件：可执行命令文件与使用说明准备好后，由用户自行在第二台机器测试核对并反馈；不要求当前 session 访问该机器。
- 状态：暂缓，尚未验证；依据 Decision 13，不阻塞当前 2.1 收尾。用户反馈后再更新环境记录和对应机器的验证结论。

### 2.2 建立参考答案与首轮基线（🔄 in_progress）

本节于 2026-09-03 完成任务分解；2.2.1–2.2.3 已于 2026-09-04 完成。fuzzy-photo 探索测试见 [environment.md](environment.md) 第 11 节；用户已将相机照片识别移出 MVP，该样本只保留为延期证据，不参与正式基线。先用已验证的 CPU 路径，第二台机器继续暂缓。

执行顺序：2.2.1 参考答案 → 2.2.2 评分工具 → 2.2.3 运行能力 → 2.2.4 OCR → 2.2.5 词级校订 → 2.2.6 单模型比较 → 2.2.7 选型。用户核对参考期间可准备评分和运行工具；正式评分只使用已核对内容。

#### 2.2.1 建立范围内样图的参考答案（✅ done）

- **完成内容：** 已为 text.png、complex-table.png、code.png 建立并核对逐区域参考；fuzzy-photo.png 已建立历史草稿，但因相机照片不再属于 MVP 而延期，不进入正式基线。
- **用户核对：** text、complex-table、code 已逐项确认；fuzzy-photo 无法可靠辨认，用户确认将相机照片识别移出 MVP。
- **逐项核对状态：** code、text、complex-table 已于 2026-09-04 经用户确认。fuzzy-photo 标记为 deferred_out_of_scope，保留不确定区域但排除 CER、模型选型和 MVP 验收。click-able 只保留可见文字；图片没有链接目标，当前设计不能恢复点击功能或补造目标。
- **格式与核对交接：** 实现时先明确并记录样本/区域 ID、正文、类型、顺序、不确定范围、核对状态及期望词级修改的最小格式，提供合成示例供评分工具复用。交给用户的清单使用稳定样本/区域标识；按实际反馈记录确认范围与版本，局部反馈不能把整页标为已核对。用户核对期间继续 2.2.2–2.2.3 的独立工具工作。
- **产物：** 本地 `sample-pic/references/` 中的 Markdown 和 `manifest.json`；记录样本 ID、图片相对路径与 SHA-256、区域类型/顺序、参考文件、不确定区域、核对状态和参考版本。不能可靠定位的内容只关联整页，不虚构精确坐标。
- **参考规则：** 忠实转写图中文字，原文本身的语言错误单独标记；另列允许的词级修改及期望初稿，区分 OCR 恢复与编辑性修改。参考和期望修改仅供评分，不传入被评估模型。
- **验收：** 三个 MVP 范围内样本均有来源明确、经用户核对的参考；延期照片有明确范围状态及不确定区域记录，不混入正式评分。

#### 2.2.2 建立评分口径与离线评分工具（✅ done）

- **我来做：** 新增 `scripts/evaluate_baseline.py`，读取参考、完整模型结果及运行记录，生成逐图 JSON 和 Markdown 报告。
- **文字评分：** 原始 OCR 对照忠实参考，校订版本对照允许词级修改后的期望稿；统计字符编辑距离/CER、漏行、重复和重点字段错误。只统一换行等无关差异，不全局删除大小写、标点、前导零或代码缩进；CER 标明分母，插入错误多时允许超过 100%。
- **结构及不确定内容：** 文本和标题/目录页码/表格行列/代码检查分开，保留原始 Markdown/HTML，格式差异不能直接算文字错误。按确认区域比对并报告覆盖率；混合不确定内容且无法可靠对齐时不强算整页 CER，保留页面遗漏/重复检查，不能通过排除不匹配输出隐藏错误。结构先做明确字段检查和集中人工核对。
- **校订评分：** 每项修改区分修复已有错误、新增错误、无收益修改和越界修改；未修复的原有错误仍计入残余错误。
- **验收：** 合成小样本覆盖插入/删除/替换、漏行/重复、不确定区域排除、前导零及代码保留；空输出、输出缺失、截断不能获得完整质量通过。真实图文继续保存在本地忽略目录。

#### 2.2.3 补齐正式比较所需的运行能力（✅ done）

- **我来做：** 扩展 `scripts/validate_model.py`、`scripts/paddleocr_child.py`；新增薄层 `scripts/run_baseline.py`，按样本清单和单条路线顺序调用已有入口，维持逐路线加载/退出。
- **输入输出：** 支持 UTF-8 文字文件及提示词文件输入；Qwen3-VL 正式运行要求完整转写，不能沿用当前 concise 提示词及默认 64-token smoke 配置。记录显式输出预算、实际生成 token 数和输入尺寸；评分读取完整输出，不能读取 run.json 中可能截短的 result_text 预览。
- **追溯：** 保留主模型/PP-DocLayoutV3 manifest、Paddle 实际子进程设备证据；补齐图片、输入文字、提示词和配置哈希、脚本 Git revision/工作区修改状态、参考版本关联。每次独立运行目录，避免读入旧结果造成假成功。
- **计时：** 分开记录哈希准备、模型加载、预处理、预测/生成、保存及总耗时；Paddle 在实际子进程计时，不能把现有含启动/加载的 generation_seconds 当纯推理时间。双模型分别记录 OCR、校订及端到端时间；先用每页独立进程的冷启动口径，不能声称连续批处理吞吐量。
- **资源：** 采样父/子进程 RSS 并记录间隔；合计值可能重复计算共享内存，是采样估计而非精确物理峰值。CPU 运行的 GPU 指标为不适用，缺失采样为未知。锁文件已有传递依赖 psutil；如实现直接使用，则显式声明并同步锁文件。
- **异常：** 可配置超时并回收该次运行的子进程树，记录失败后继续下一图；运行成功与质量通过分开记录。检查输出预算、疑似截断、明显重复及空输出；达到预算是检查信号，不能直接判定一定截断，提前停止也不证明完整。按明确原因最多调整后重试一次，两次记录均保留。
- **验收：** 合成子进程覆盖超时、非零退出、空输出、独立目录及记录完整性；随后真实单图确认新增输入/提示词、计时和设备记录。只验证相关风险，不重复下载已有模型。

#### 2.2.4 运行 PaddleOCR-VL 三样本识别基线（⬜ pending）

- **我来做：** 用 text、complex-table、code 三张范围内原图逐图识别，保存原始结果、设备/配置、分阶段耗时及资源采样；评分并集中检查结构。
- **条件：** 首轮不预处理、不校订；后续路线使用同一组三个样本和参考版本。已有 fuzzy-photo 结果仅为延期能力的探索记录，不混入正式比较。
- **验收：** 三个范围内样本各有成功或明确失败记录、完整性检查及可核对输出；列出漏字/漏行、专业名称、表格数字和代码问题。不能排除失败样本后只汇报成功样本均值。

#### 2.2.5 验证 Qwen3-4B 轻量词级校订（⬜ pending）

- **我来做：** 新增 `scripts/correct_text.py`，读取 Paddle 完整初稿，按区域/内容类别调用已有文字模型；保存建议、实际应用/拒绝项、校订稿和增量耗时。采用有限区域输入，无法可靠区分的代码/数据区域保守保留并记录。
- **代码边界：** 模型给出有位置和原文依据的最小修改；程序验证位置、原文匹配、不重叠及保护范围，再生成新版本。格式无效或越界时保留原稿并记录，不能把模型建议默认视为正确。
- **保护用例：** `dup1icate → duplicate`、`becuase → because`；`It was make by me.` 保留；技术名称、代码标识符、编号和前导零不随意改写。按内容类别保护数字，不能一概禁止修改含数字的普通 OCR 错词。
- **验收：** 合成测试验证差异应用和保护规则，再对三个范围内样本的初稿实测纠错收益、新增错误、越界修改和总耗时。用例不构成穷尽保证；净收益不足时如实记录，不提前宣称有效。

#### 2.2.6 运行 Qwen3-VL 单模型比较（⬜ pending）

- **我来做：** 使用同一组三个范围内样本和参考版本，按第一轮词级边界设置提示词及整页输出预算，测量单模型直接产出初稿的质量、结构、耗时和资源，记录该模式与纯转写的区别。
- **重点：** 分开比较 Paddle 原始 OCR、Paddle + 校订、Qwen3-VL 初稿，不能用短摘要与完整转写比较后声称更快。参数调整遵循 2.2.3 重试上限；fuzzy-photo 不参与。
- **追溯限制：** 单次视觉输出没有独立校订前文本，修改因果归属记为不可测；只比较最终误差和保护项，不编造修复数。若入选，Phase 3 前须解决识别初稿与修改记录契约；额外生成的成本另计。
- **验收：** 三个范围内样本都有结果或失败原因，资源、耗时、覆盖范围及完成性可对照；结论不扩展到相机照片、真正复杂表格或多页材料。

#### 2.2.7 汇总初步选型和验收指标（⬜ pending）

- **我来做：** 汇总至 `docs/baseline.md`，呈现三种输出的逐图质量、OCR/校订/总耗时、内存、失败类型及配置，给出有证据的建议和剩余问题。报告保留必要指标与问题说明，不自动将本地整页转写加入 Git。
- **你来做：** 看少量代表性前后对照，判断残余错误和每页/整批等待是否可接受，确认初步路线取舍；不需要审日志、计算指标或写脚本。可以提前提供常见批次页数及期望等待时间，没有则先看实测，不编造阈值。
- **验收：** 形成当前机器 CPU 范围内的模型/后端/配置结论及用户认可的速度与质量目标。候选都不满足时，列出具体问题和下一项有针对性的配置/预处理实验，不勉强选型或直接进入 Phase 3。
- **收尾：** 据实更新任务和 current.md；确认了选型或重要取舍再追加 decision.md 并同步当前设计。第二台机器及 GPU 路径继续单列为未验证，后续按用户安排实测。

#### 2.2 预计代码改动与协作方式

下列为拟实施文件，不代表已创建；拆文件方式可按复用情况收敛，不引入产品框架。

| 文件 | 改动及目的 |
| --- | --- |
| `scripts/validate_model.py`（已有） | 文字/提示词文件输入、完整输出、token/阶段时间/异常记录；复用模型加载与 manifest |
| `scripts/paddleocr_child.py`（已有） | 实际推理进程内加载/预测/保存计时及设备记录 |
| `scripts/run_baseline.py`（拟新增） | 样本/路线顺序运行、独立输出目录、超时、进程资源采样与汇总 |
| `scripts/evaluate_baseline.py`（拟新增） | 确认区域评分、重点字段/完整性检查、比较报告 |
| `scripts/correct_text.py`（拟新增） | 词级修改建议、保护/应用规则、修改记录及校订稿 |
| `tests/`（拟新增，标准库 unittest） | 合成数据验证评分、修改边界和失败处理，不用模型逐字输出作脆弱断言 |
| `pyproject.toml` / `uv.lock`（按需） | 新增直接依赖时同步声明/锁定，其余配置沿用 |

- 我负责参考草稿、实现、运行、评分与报告；你主要在参考草稿形成后集中核对一次，在报告形成后集中判断一次。不确定区域可排除并标明，不为每个疑点中断任务。
- 真实图片及参考放在 Git 忽略的 `sample-pic/`，正式运行放在 `validation-output/baseline/<run-id>/`；样本缺失时报错，不假定新检出环境自带样图。参考是重复评估输入，不能当一次性输出清理。
- 评分规则、提示词、合成样本及必要评估配置纳入版本控制；真实模型路径/本地参数由命令行传入，不写死用户缓存目录。
- 新 session 的工作目录/未提交文档交接、环境预检、用户核对方式及第一批结束条件见 [current.md](current.md) 的“Task 2.2 — 新 session 启动与交接”。
- 第一批实施建议为 2.2.1–2.2.3；参考核对和工具验证完成后，按后续指令进入四图实跑。本次只做任务分解，不安装依赖、不运行模型、不实现上述代码，也不改变既定 MVP 设计。

## Phase 3 — CLI 手动导入 MVP（⬜ pending）

### 3.1 图片输入与任务管理（⬜ pending）

- ⬜ 确定 CLI 命令契约、输入格式及运行数据保存方式。
- ⬜ 实现 CLI 多图导入、明确页序与可选低成本图像校正，保留原图。
- ⬜ 实现文档和页面状态、进度保存、中断恢复及单页重试。

### 3.2 快速识别与轻量词级校订（⬜ pending）

- ⬜ 接入选定识别方案，保存阅读顺序、区域来源及识别初稿。
- ⬜ 实现按内容类别的轻量词级纠错，保护技术名称、表格数据和代码，保留句子级语法及语义问题。
- ⬜ 保存修改差异、页面来源、失败记录及已有问题，提供 CLI 进度和批次汇总，不逐项等待人工确认。

### 3.3 Markdown 输出（⬜ pending）

- ⬜ 合并页面内容，保留标题、段落、列表、代码和页面来源。
- ⬜ 实现普通 Markdown 表格与复杂表格的 HTML/图片表示，检查配套资源链接。
- ⬜ 在目标查看器中核对导出文件，保留识别初稿、轻量校订版本及修改记录。

## Phase 4 — 多页与困难样本验收（⬜ pending）

### 4.1 扩充样本与批量验证（⬜ pending）

- ⬜ 补充连续几十页材料、真正复杂表格及可核对的参考答案。
- ⬜ 检查漏页、漏行、重复内容、跨页段落和表格对应关系。
- ⬜ 验证失败页重试、中断恢复和资源使用，确认实际批量适用范围。

### 4.2 质量与用户验收（⬜ pending）

- ⬜ 测量清晰截图/扫描图中的技术名称误改、数字和代码符号错误，核对来源及已有问题记录；相机照片识别不属于 MVP 验收。
- ⬜ 按基线确定的速度与误差取舍验收初稿流程，记录未解决问题；第一轮不要求发现或修复全部语法语义错误。
- ⬜ 分别记录自动验证结果及用户在目标机器上的手动使用验收。

## Phase 5 — 后续扩展（非 MVP，⬜ pending）

### 5.1 自动采集（⬜ pending）

- ⬜ 细化并实现自动截图、翻页、区域覆盖和重复检测，接入现有导入流程。

### 5.2 PDF 输出（⬜ pending）

- ⬜ 选择并实现 Markdown 转 PDF，验证复杂表格、代码、图片和分页。

### 5.3 深入二次校验与按需人工复核（⬜ pending）

- ⬜ 针对已保存初稿增加独立的句子级语法和语义检查，验证 It was make by me. 等第一轮保留的问题。
- ⬜ 集中反馈二次校验疑点，支持按需原图对照、局部重新识别及人工调整，评估轻量核对报告的需要。
- ⬜ 记录二次校验纠错收益、新增错误及人工介入量，评估复用文字模型是否足够。

Phase 5 的各类扩展相互独立，实施先后按后续任务指令确定；当前均未开始。
