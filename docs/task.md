# Tasks

Work is organized in three levels: **Phase → Task → SubTask**. Phases are sequential. Preserve task order when checkpointing; completion requires evidence.

| Symbol | Status | Meaning |
| --- | --- | --- |
| ⬜ | pending | 尚未开始 |
| 🔄 | in_progress | 已开始，尚未完成 |
| ✅ | done | 有明确完成证据 |
| ⏸️ | blocked | 已确认的阻塞，需注明原因 |

Task/Phase 状态由 SubTask 推导：有进行中项则为 in_progress；无进行中项而存在阻塞则为 blocked；全部完成才为 done；尚未开始为 pending。下列后续工作均只记录计划，当前没有启动实现或模型验证。

## Phase 1 — 方案与上下文初始化（✅ done）

### 1.1 需求与设计整理（✅ done）

- ✅ 确认 CLI 为 MVP 正式入口，手动批量导入图片，自动截图与翻页留到后续。
- ✅ 确认英文文档优先，兼顾表格和少量代码，几乎无手写内容。
- ✅ 确认速度优先，MVP 包含基本排版与轻量词级纠错，深入句子语法和语义留到二次校验。
- ✅ 确认 Markdown 输出及后续 PDF 扩展，记录复杂表格表示方案。
- ✅ 确认 Python 3.12 和后续 uv 初始化；记录两台机器的已知配置。
- ✅ 整理双模型优先验证、单模型对比及按页处理架构。

### 1.2 样图评估与文档保存（✅ done）

- ✅ 目视查看 text.png、fuzzy-photo.png、complex-table.png 和 code.png。
- ✅ 记录摩尔纹、技术名称、表格前导零及代码符号风险；注明现有表格样本不覆盖复杂表格。
- ✅ 初始化五份文档并保存 checkpoint；同步 CLI、无人值守批处理和两轮校验边界。

## Phase 2 — 本地模型可行性验证（⬜ pending）

### 2.1 初始化验证环境（⬜ pending）

- ⬜ 在后续实施开始时核对两台目标机器的系统、CPU、GPU、显存及 Python 3.12 兼容性。
- ⬜ 使用 uv 初始化 Python 3.12 项目，确定验证阶段依赖和可复现的运行方式。
- ⬜ 准备选定的本地模型与推理环境，验证 CPU 或 NVIDIA 推理路径。

### 2.2 建立参考答案与首轮基线（⬜ pending）

- ⬜ 为四张样图建立人工核对的参考转写，标明不确定区域和结构。
- ⬜ 运行 PaddleOCR-VL 识别基线，保存逐页输出、配置和资源指标。
- ⬜ 增加小型文字模型词级校订，分别统计纠错收益、新增错误及增量耗时，验证句子级语法留到第二轮的边界。
- ⬜ 运行 Qwen3-VL 单模型对比，比较产出初稿的速度、基本结构和轻量纠错效果。
- ⬜ 按速度优先和可接受误差的目标，确定初步模型组合、推理后端、资源配置及验收指标。

## Phase 3 — CLI 手动导入 MVP（⬜ pending）

### 3.1 图片输入与任务管理（⬜ pending）

- ⬜ 确定 CLI 命令契约、输入格式及运行数据保存方式。
- ⬜ 实现 CLI 多图导入、明确页序与可选低成本图像校正，保留原图。
- ⬜ 实现文档和页面状态、进度保存、中断恢复及单页重试。

### 3.2 快速识别与轻量词级校订（⬜ pending）

- ⬜ 接入选定识别方案，保存阅读顺序、区域来源及识别初稿。
- ⬜ 实现按内容类别的轻量词级纠错，保护技术名称、表格数据和代码，保留句子级语法及语义问题。
- ⬜ 保存修改差异、页面来源、失败记录及已有问题，提供 CLI 进度和批次汇总，不逐项等待人工确认。

### 3.3 Markdown 输出（⬜ pending）

- ⬜ 合并页面内容，保留标题、段落、列表、代码和页面来源。
- ⬜ 实现普通 Markdown 表格与复杂表格的 HTML/图片表示，检查配套资源链接。
- ⬜ 在目标查看器中核对导出文件，保留识别初稿、轻量校订版本及修改记录。

## Phase 4 — 多页与困难样本验收（⬜ pending）

### 4.1 扩充样本与批量验证（⬜ pending）

- ⬜ 补充连续几十页材料、真正复杂表格及可核对的参考答案。
- ⬜ 检查漏页、漏行、重复内容、跨页段落和表格对应关系。
- ⬜ 验证失败页重试、中断恢复和资源使用，确认实际批量适用范围。

### 4.2 质量与用户验收（⬜ pending）

- ⬜ 测量模糊照片残余错误、技术名称误改、数字和代码符号错误，核对来源及已有问题记录。
- ⬜ 按基线确定的速度与误差取舍验收初稿流程，记录未解决问题；第一轮不要求发现或修复全部语法语义错误。
- ⬜ 分别记录自动验证结果及用户在目标机器上的手动使用验收。

## Phase 5 — 后续扩展（非 MVP，⬜ pending）

### 5.1 自动采集（⬜ pending）

- ⬜ 细化并实现自动截图、翻页、区域覆盖和重复检测，接入现有导入流程。

### 5.2 PDF 输出（⬜ pending）

- ⬜ 选择并实现 Markdown 转 PDF，验证复杂表格、代码、图片和分页。

### 5.3 深入二次校验与按需人工复核（⬜ pending）

- ⬜ 针对已保存初稿增加独立的句子级语法和语义检查，验证 It was make by me. 等第一轮保留的问题。
- ⬜ 集中反馈二次校验疑点，支持按需原图对照、局部重新识别及人工调整，评估轻量核对报告的需要。
- ⬜ 记录二次校验纠错收益、新增错误及人工介入量，评估复用文字模型是否足够。

Phase 5 的各类扩展相互独立，实施先后按后续任务指令确定；当前均未开始。
